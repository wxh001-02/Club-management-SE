<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sau.mapper.ClubMapper">
    <select id="findById" resultType="cn.sau.domain.Club">
        SELECT c.*, u.username as president_name
        FROM club c
        LEFT JOIN user u ON c.president_id = u.id
        WHERE c.id = #{id}
    </select>

    <select id="findByName" resultType="cn.sau.domain.Club">
        SELECT * FROM club WHERE name = #{name}
    </select>

    <select id="findAll" resultType="cn.sau.domain.Club">
        SELECT c.*, u.username as president_name
        FROM club c
        LEFT JOIN user u ON c.president_id = u.id
        ORDER BY c.create_time DESC
    </select>

    <select id="findByPresidentId" resultType="cn.sau.domain.Club">
        SELECT c.*, u.username as president_name
        FROM club c
        LEFT JOIN user u ON c.president_id = u.id
        WHERE c.president_id = #{presidentId}
        ORDER BY c.create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO club (name, logo, description, created_date, status, president_id, create_time, update_time)
        VALUES (#{name}, #{logo}, #{description}, #{createdDate}, #{status}, #{presidentId}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE club
        SET name = #{name},
            logo = #{logo},
            description = #{description},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM club WHERE id = #{id}
    </delete>
</mapper>
