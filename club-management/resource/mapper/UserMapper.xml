<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sau.mapper.UserMapper">
    <select id="findById" resultType="cn.sau.domain.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findByUsername" resultType="cn.sau.domain.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findAll" resultType="cn.sau.domain.User">
        SELECT * FROM user ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, role, create_time, update_time)
        VALUES (#{username}, #{password}, #{role}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE user
        SET username = #{username},
            role = #{role},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="findByUsernameAndPassword" resultType="cn.sau.domain.User">
        SELECT * FROM user WHERE username = #{username} AND password = #{password}
    </select>
</mapper>
